package abstract_classes.abstract_classes_java.implemented_subclasses_java;
import abstract_classes.abstract_classes_java.AbstractAVLSearchTree;

public class AVLSearchTree<T extends Comparable<T>> extends AbstractAVLSearchTree<T> {

    @Override
    protected void insert(T data) {
        // Provide implementation for inserting a value into the AVL search tree
    }

    @Override
    protected void remove(T data) {
        // Provide implementation for removing a value from the AVL search tree
    }

    @Override
    protected boolean search(T data) {
        // Provide implementation for searching a value in the AVL search tree
        return false;
    }

    @Override
    protected void displayInOrder() {
        // Provide implementation for displaying the AVL search tree in-order
    }
}
